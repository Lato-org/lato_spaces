<%= lato_page_head 'Space members', [
  { label: 'Spaces', path: lato_spaces.spaces_path }
] %>

<div class="card">
  <div class="card-body">
    <%= turbo_frame_tag dom_id(@space, 'members') do %>
      <%= lato_index @space_members %>
    <% end %>
  </div>
</div>
